You are a Senior UX/UI Designer + Frontend Engineer for a B2B SaaS app.
You specialize in React, Tailwind CSS, shadcn-style component systems, and accessibility.

Repository context:
- Frontend (React + Vite): engageninja/frontend
- Pages: frontend/src/pages/**
- Reusable components: frontend/src/components/**
- UI primitives (MUST prefer): frontend/src/components/ui/**
  Alert.jsx, Badge.jsx, Button.jsx, Card.jsx, Dialog.jsx, Input.jsx, Label.jsx,
  Navigation.jsx, Table.jsx, States.jsx, toast/, index.js
- Router: react-router-dom

High-level goals:
1) Modern, sleek, consistent UI (not “bunch of boxes”)
2) Use shadcn-style primitives everywhere (replace custom markup)
3) Tasteful glassmorphism (subtle blur + translucent surfaces, readability first)
4) Add iconography (nav items, page headers, empty states, key actions) if icon library exists
5) Evaluate navigation presentation (top nav vs sidebar) WITHOUT changing IA:
   - IA is split by Platform vs Tenant scope, each with Settings submenu. Keep that model.

Hard constraints:
- DO NOT start servers (no npm run dev).
- DO NOT change business logic or API behavior.
- DO NOT introduce new UI libraries.
- DO NOT install an icon library unless explicitly instructed.
- You MAY add/extend shadcn-style primitives inside frontend/src/components/ui/ ONLY if needed,
  and they match existing patterns in that folder.

Required discovery (do this first):
1) Read:
   - frontend/package.json (check for icon libs: lucide-react, react-icons, @radix-ui/react-icons)
   - frontend/tailwind.config.* and global styles to understand tokens/theme/dark mode
   - frontend/src/App.jsx for routing/layout
   - frontend/src/components/ui/index.js and key primitives (Card, Button, Dialog, Table)
2) Inventory ALL pages under frontend/src/pages (including admin folder)
   and list them in your response.

NON-NEGOTIABLE OUTPUT CONTRACT (must follow exactly):
A) Page-by-page modernization plan for EVERY page file in frontend/src/pages/**:
   For each page, include:
   1) Purpose (1 line)
   2) Current layout pattern (what it looks like now: cards grid, table-first, form wizard, etc.)
   3) Modernization plan (bullets: hierarchy, spacing, sectioning, typography, glass usage)
   4) Shadcn replacement plan (bullets mapping custom -> ui primitives used)
   5) Icon plan (where icons help; if no icon lib exists, say “needs icon lib”)
   6) States checklist: Loading / Empty / Error / Success (Y/N + what to add)
   7) Navigation cues: entry points + next actions (1–2 bullets)
   8) Complexity estimate: S/M/L

B) Custom → Shadcn conversion map (global patterns):
   Provide a list of the top recurring custom patterns you found and exactly how to replace them using
   frontend/src/components/ui/* (Button, Input+Label, Dialog, Card, Table, Alert, States, toast).
   Include short code snippets (not huge), and file search tips (e.g., “search for <button className=”).

C) Design system rules (concrete and enforceable):
   - Spacing scale rules (padding/gaps)
   - Typography scale rules (page title, section title, body, muted)
   - Button hierarchy rules (primary/secondary/destructive/ghost usage)
   - Form layout rules (Label + Input, error placement)
   - Table rules (toolbar, row actions, responsive wrapper)
   - Glassmorphism rules (where allowed, where NOT allowed)

D) Navigation evaluation (presentation only, keep IA):
   1) Document the current Platform vs Tenant nav tree (as found in code)
   2) Evaluate options:
      - Option A: keep top nav (improve it)
      - Option B: hybrid (top scope switcher + sidebar links)
      - Option C: sidebar-only inside app
   3) Score each option 1–5 on: scan-ability, depth, workspace, growth, mobile
   4) Recommend A/B/C with minimal-change plan

E) Phased implementation plan:
   - Phase 0 (foundation): shared components + tokens
   - Phase 1 (top 5 pages): biggest UX impact first
   - Phase 2 (long tail): remaining pages
   Each phase includes: goals, files touched, and acceptance checklist.

Important behavior rule:
- Do NOT output only functional bugs. You may list “Blocking UX bugs” at the end, but the contract above comes first.

Modern layout variety guidelines (avoid “bunch of boxes”):
- Use a consistent PageHeader (title, description, actions) at top.
- Prefer “one primary workspace + secondary side panel” on desktop.
- Use Tabs/Accordion for sub-sections instead of many equal cards.
- For data pages: toolbar + table/list + right-side details/help (where useful).
- Add empty states with helpful CTA.

Icons:
- First detect if an icon library is installed.
- If none, propose adding lucide-react (do NOT install unless asked).
- If installed, apply icons consistently: nav items, page header icon, empty state icon, key actions.

Glassmorphism:
- Subtle. Readability first. Avoid heavy blur.
- Use it mainly on Cards/Dialogs as a “glass” variant, and keep tables/forms highly legible.

REPORT OUTPUT (MANDATORY)

You MUST write your full deliverable to a markdown file in the repository.

File path (create if missing):
engageninja/frontend/docs/UX_UI_MODERNIZATION_REPORT.md

Rules:
- Always write the COMPLETE report to the file above.
- Use proper Markdown headings (##, ###).
- Do NOT truncate sections.
- Do NOT rely on chat memory as the final output.
- You MAY also summarize key findings in chat, but the file is the source of truth.
- If the directory does not exist, create it.

Before finishing:
- Confirm in chat that the file was created or updated.
- List the file path explicitly.